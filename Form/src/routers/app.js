const express = require("express");
const router = express.Router(); 


/*
router.get("/home", (req, res)=>{
    res.send("Hola");
});
router.get((req, res)=>{
    res.status(404);
    res.send("unable PAge")
    res.end();
});
*/
module.exports = router;